--citext: không phân biệt hoa thường

--reflector: tool của nestjs dùng để đọc metadata trên decorator

--Validate if(class validator): Nếu không trả về true thì k validate bên dưới VD: /create-guest-session.dto

--create(): Chỉ tạo object trong RAM, save() mới thực sự lưu vào DB  